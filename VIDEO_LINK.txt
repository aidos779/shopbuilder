Demo Video Link

VIDEO_URL=https://youtu.be/SQiDaDUyxaM

The video must show:
1. `docker compose up --build` booting PostgreSQL, Redis, backend, worker, and frontend.
2. `curl http://localhost:3000/health` returning ok.
3. Swagger at `/docs`.
4. `npm test` passing.
5. `npm run verify:email` sending a real SMTP test email.
6. Merchant-owner registration, email verification, login, product creation, variant generation.
7. Warehouse creation, warehouse stock set, and smart inventory checkout.
8. Discount code creation and checkout discount application.
9. Customer order confirmation and merchant order notification email events.
10. Payment intent, 3D Secure flow, and capture.
11. Webhook endpoint creation, HMAC-signed delivery attempt, retry/DLQ visibility.
12. Storefront token creation and scoped `/storefront/products` call.
13. Subscription creation and renewal billing email worker.
14. Analytics endpoint and frontend analytics view.
15. `/admin/queues` queue visibility.
